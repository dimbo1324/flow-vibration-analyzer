# NOTE: The application version is defined solely in iva/__version__.py
# (the single source of truth). It is intentionally NOT duplicated here.
[application]
name = "Industrial Vibration Analyzer"

[paths]
examples_dir = "data/examples"
synthetic_dir = "data/synthetic"
results_dir = "data/results"

[logging]
level = "INFO"
